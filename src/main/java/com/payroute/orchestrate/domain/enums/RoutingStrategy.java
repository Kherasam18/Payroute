package com.payroute.orchestrate.domain.enums;

/**
 * Defines the available routing strategies for provider selection.
 */
public enum RoutingStrategy {
    PRIORITY,
    COST_OPTIMIZED,
    SUCCESS_RATE
}
